<p>
  <p><b>Comment By:</b> <%= comment.user.first_name %><p>
  <%= link_to "Edit Comment", edit_user_post_path(comment.user, comment.post, comment) %>
  <%= link_to "Delete Comment", [comment.user, comment.post, comment], method: :delete,
              data: { confirm: "Are you sure?" }  %>
<p>
<p>
  <%= comment.content %>
<p>